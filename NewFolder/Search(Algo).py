import math

#def binarySearch(A, x):
 #   if x == A[math.floor(len(A)/2)]:
  #      print('', A[math.floor(len(A)/2)])
   #     return True
    #if x < A[math.floor(len(A)/2)]:
     #   binarySearch(A[math.floor(len(A)/2) :], x)
    #if x > A[math.floor(len(A)/2)]:
    #    binarySearch(A[: math.floor(len(A)/2)], x)

n = 2
threshold = int(input())
for n in range(2,7):
    if n >= threshold:
        print('_')
        continue






